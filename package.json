{
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm stub",
    "lint": "eslint \"**/*.{ts,vue,mjs}\"",
    "lint:fix": "pnpm lint -- --fix",
    "release": "bumpp package.json packages/*/package.json --commit --push --tag",
    "stub": "pnpm -r --parallel run stub",
    "test": "pnpm run test:vitest && pnpm run test:pest",
    "test:vitest": "vitest test --run",
    "test:pest": "composer test"
  },
  "devDependencies": {
    "@innocenzi/eslint-config": "^0.12.0",
    "@hybridly/core": "workspace:*",
    "@hybridly/vite": "workspace:*",
    "@hybridly/vue": "workspace:*",
    "@types/debug": "^4.1.7",
    "@types/throttle-debounce": "^5.0.0",
    "@vitest/ui": "^0.22.1",
    "bumpp": "^8.2.1",
    "eslint": "^8.23.0",
    "happy-dom": "^6.0.4",
    "hybridly": "workspace:*",
    "msw": "0.45.0",
    "type-fest": "^2.19.0",
    "typescript": "^4.8.2",
    "unbuild": "0.8.9",
    "vite": "^3.0.9",
    "vitest": "^0.22.1"
  },
  "pnpm": {
    "overrides": {
      "unbuild": "0.6.9"
    }
  }
}
